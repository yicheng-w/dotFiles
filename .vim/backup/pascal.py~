

def genRow(n):
    row = [1]
    newRow = [1]
    i = 0
    while i < n:
        print row
        newRow = [1]
        for i in range(0, len(row) - 1):
            newRow.append((row[i] + row[i + 1]) % 2)
        newRow.append(1)
        row = newRow
    return sum(newRow)

print genRow(1000)
